{
  "name": "multi-admin",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:server": "pnpm --filter @multi-admin/nestjs-server run dev",
    "dev:mobile": "pnpm --filter @multi-admin/uni-mobile run dev:h5",
    "dev:web": "pnpm --filter @multi-admin/pure-web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "format": "prettier --write .",
    "typecheck": "pnpm -r run typecheck",
    "clean:cache": "node ./scripts/clean.mjs --del-lock && pnpm install",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "wewant-rocky",
  "license": "ISC",
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "typescript": "catalog:",
    "prettier": "catalog:"
  }
}
